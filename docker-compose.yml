version: '3'
services:

  client:
    image: cedardevs/onestop-client:latest
    ports:
     - 8080:80
    depends_on:
      - api-search

  # api:
  #   build:
  #     context: ./api/
  #     args:
  #       VERSION: ${VERSION}
  #       DATE: ${DATE}
  #       VCS_REF: ${VCS_REF}
  #       JAR_NAME: onestop-api-all
  #   ports:
  #     - 8097:8097
  #   depends_on:
  #     - elasticsearch
  #   environment:
  #    - elasticsearch_host=elasticsearch
  #    - elasticsearch_port=9300

  api-search:
    image: cedardevs/onestop-api-search:latest
    ports:
      - 8097:8097
    depends_on:
      - elasticsearch
    environment:
     - elasticsearch_host=elasticsearch
     - elasticsearch_port=9200

  # api-metadata:
  #   image: cedardevs/onestop-api-metadata:latest
  #   ports:
  #     - 8097:8097
  #   depends_on:
  #     - elasticsearch
  #   environment:
  #    - elasticsearch_host=elasticsearch
  #    - elasticsearch_port=9200

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:${ES_VERSION:-latest}
    environment:
     - xpack.security.enabled=false
    ports:
     - 9300:9300
     - 9200:9200

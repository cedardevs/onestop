apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-admin.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "api-admin.name" . }}
    helm.sh/chart: {{ include "api-admin.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "api-admin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

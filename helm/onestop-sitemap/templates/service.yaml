apiVersion: v1
kind: Service
metadata:
  name: {{ include "onestop-sitemap.fullname" . }}
  labels:
    {{- include "onestop-sitemap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "onestop-sitemap.selectorLabels" . | nindent 4 }}

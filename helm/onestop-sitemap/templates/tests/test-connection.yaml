apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onestop-sitemap.fullname" . }}-test-connection"
  labels:
    {{- include "onestop-sitemap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "onestop-sitemap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

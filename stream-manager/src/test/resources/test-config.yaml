kafka:
  bootstrap:
    servers: "http://bootstrap-servers:3000"
  schema:
    registry:
      url: "http://schema-registry:5000"
  compression:
    type: "snappy"
  cache:
    max:
      bytes:
        buffering: 209715200
  commit:
    interval:
      ms: 50000
  auto:
    offset:
      reset: "latest"
  producer:
    one: "testproducer1"
    two: "testproducer2"
  consumer:
    one: "testconsumer"
elasticsearch:
  setting: "this will not be saved"
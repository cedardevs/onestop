CRUD RESTFul APIs for a Save search user Management System using Spring Boot 2 JPA and PostgreSQL database. 
Following are the REST APIs (Controller handler methods) are created for SaveSearch resource.
